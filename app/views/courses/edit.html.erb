<%= page_header t('views.courses.edit') %>
<% if policy(@course).update? %>
  <%= render 'form', course: @course %>

  <%= link_to t('links.destroy'), course_path, method: :delete,
    data: { confirm: 'Hey, sure?' },
    class: 'btn btn-danger' %>
<% end %>
