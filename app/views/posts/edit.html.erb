<h1><%= t('views.posts.edit_news') %></h1>
<% if policy(@post).update? %>
  <%= render 'form' %>

  <%= link_to t('links.show'), @post %> |
  <%= link_to t('links.back'), posts_path %>
  <%= link_to t('links.destroy'), post_path, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% end %>
